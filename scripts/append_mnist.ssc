import BIDMat.MatIO
import BIDMat.HDFSIO
import BIDMat.SerText

val prefix = "hdfs://localhost:9000/BIDMach_MNIST/parts/"
val fnames = (0 to 80).map(i => (prefix + "alls%02d.fmat.lz4" format i)).toList

val hdfsio = new HDFSIO
hdfsio.appendFiles(fnames, "hdfs://localhost:9000/BIDMach_MNIST/merged.fmat.lz4", 2);


